# Claude Code Repository Context

Read `.github/context/README.md` and all linked context documents before making changes. They define the verified current status, prioritized next steps, architecture, security boundaries, and release workflow.

Do not attempt to bypass Claude CLI authentication or access provider credential files. Update the shared context after material changes so Copilot, Codex, Claude, and other assistants receive the same handoff.

